default:
    suites:
        domain:
            contexts:
                - ApiContext:
                    testMode: true
            filters:
                tags: '~@integration'
        integration:
            contexts:
                - ApiContext:
                    testMode: false
            filters:
                tags: '@integration'
