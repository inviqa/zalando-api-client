default:
    suites:
        domain:
            contexts:
                - ApiContext:
                    testMode: true
        integration:
            contexts:
                - ApiContext:
                    testMode: false
